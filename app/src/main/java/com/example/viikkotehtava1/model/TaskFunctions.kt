package com.example.viikkotehtava1.model
/*
fun addTask(list: List<Task>, task: Task) : List<Task>{
    return list + task
}

fun toggleDone(list: List<Task>, id: Int) : List<Task>{
    list.forEach { it ->
        if (it.id == id){
            it.done = true
        }
    }
    return list
}

fun filterByDone(list: List<Task>, done: Boolean) : List<Task>{
    val doneTasks = list.filter { it.done }
    return doneTasks
}

fun sortByDueDate(list: List<Task>) : List<Task>{
    val byDueDate = list.sortedBy { it.dueDate }
    return byDueDate
}
*/